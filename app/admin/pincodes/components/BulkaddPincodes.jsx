"use client";
import { x, upload } from "lucide-react";

export default function BulkUPloadModal({
    open, 
    onClosw,
    file,
    setFile,
    handleUpload,
    uploading,
}) {
 if(!open) return null;
 return (
    
 )   
}